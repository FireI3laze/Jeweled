package de.foldingfan.firstmod.capability.maxhealth;

import net.minecraft.nbt.CompoundTag;
import net.minecraftforge.common.capabilities.ICapabilityProvider;
/*
public class PlayerMaxHealth implements ICapabilityProvider {
    private int maxHealth;

    private final int MIN_MAXHEALTH = 5;
    private final int MAX_MAXHEALTH = 15;


    public void addMaxHealth(int add) {
        this.maxHealth = Math.max(maxHealth + add, MAX_MAXHEALTH);
    }

    public void subMaxHealth(int sub) {
        this.maxHealth = Math.min(maxHealth - sub, MIN_MAXHEALTH);
    }

    public void copyFrom(PlayerMaxHealth source) {
        this.maxHealth = source.maxHealth;
    }

    public void saveNBTData(CompoundTag nbt) {
        nbt.putInt("maxHealth", maxHealth);
    }

    public void loadNBTData(CompoundTag nbt){
        maxHealth = nbt.getInt("maxHealth");
    }
}

 */
